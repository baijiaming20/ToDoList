import { Language } from '@/utils/type'
import { createStore } from 'vuex'

export default createStore({
  state: {
    // setting: {
    //   language: Language.zh
    // }
  },
  mutations: {
    // 改变语言设置
    // changeLanguage(state, lang) {
    //   state.setting.language = lang
    // }
  },
  actions: {
  },
  modules: {
  }
})
